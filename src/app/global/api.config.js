export const apiConfig = {
    base_api_host:"api/v1/",         //(../)为相对与dist目录     SuperviseSystem/
    allowUrls : "login",        //不需要添加token请求的接口
    noAppToken:"\/examinationLogin",     //访问指定页面时不需要token验证
} // conf/db.js
